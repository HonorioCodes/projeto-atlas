class WorkoutModel {
  final String title;
  final String duration;
  final String description;

  const WorkoutModel({
    required this.title,
    required this.duration,
    required this.description,
  });
}